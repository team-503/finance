version: 2

updates:
    - package-ecosystem: npm
      directories:
          - /
          - /apps/*
          - /packages/*
      schedule:
          interval: weekly
      open-pull-requests-limit: 1
      groups:
          npm:
              patterns:
                  - '*'

    - package-ecosystem: github-actions
      directory: /
      schedule:
          interval: weekly
      open-pull-requests-limit: 1
      groups:
          actions:
              patterns:
                  - '*'
